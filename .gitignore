# Auth & secrets
auths/
data/
*.key
*.pem
*.env
.env
.env.*

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
/login
/credit
/wb2api
/signin_bin

# Config with secrets (use config.example.json)
config.json

# Build artifacts
*.tmp
__pycache__/
# Windows 下 `go build ./cmd/server` 会在仓库根留下 server.exe
/server.exe
/wb2api.exe

# 运行时产物（API Key / 进程号 / 日志 / 工具草稿目录）
# 为什么显式列 .apikey：它不是 config.json 的别名，而是 login.sh 等工具单独落盘的密钥文件，
# 不写进这里会被 git add -A 直接提交（含明文 api_key）。
.apikey
.server.pid
*.log
draft_*_folder/

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md
docs/

# 设计/计划文档（但 README.md 必须进版本库）
*.md
!README.md
# 面板（panel/，git subtree）是独立项目，其文档需随 subtree 一起进出，
# 否则每次 pull 都会静默丢掉作者的 README / 文档更新。
# 注意 docs/ 是「目录」级排除，仅靠 !panel/**/*.md 无法救回其中的文件——
# git 不允许重新包含被排除目录内的文件，必须先重新包含目录本身。
!panel/README.md
!panel/**/*.md
!panel/docs/
!panel/docs/**
# panel/host 是合并时新增的装配包（非上游面板文件），其文档同样需要入库
!panel/host/*.md

# go.work：多 module 工作区文件。本项目用 go.mod 的 replace 而非 work 文件
# （replace 随仓库分发、构建确定；work 文件会覆盖 replace 且通常在 .gitignore 外，
# 一旦误提交会让所有人的构建行为与 CI 不一致）。此处显式忽略以防误提交。
go.work
go.work.sum
